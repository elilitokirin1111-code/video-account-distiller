# Instagram mapping

Use only user exports. Common aliases include `user_id`/`ig_user_id`, `username`, `media_id`,
`timestamp`, `play_count`, `like_count`, `comments_count`, `shares`, and `saved`. Do not treat reach,
impressions, and plays as interchangeable.
