# WeChat Channels mapping

Use only user exports. Common aliases include `finder_id`, `object_id`/`feed_id`, `read_count` or
`play_count`, `like_count`, `comment_count`, `share_count`, and `favorite_count`. Preserve the export
definition of reads/views in source metadata.
