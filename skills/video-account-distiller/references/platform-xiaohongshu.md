# Xiaohongshu mapping

Use only user exports. Common aliases include `user_id`/`red_id`, `note_id`, `liked_count`,
`comment_count`, `share_count`, and `collected_count`. Keep collection/saves distinct from likes and
leave unavailable view fields as `null`.
