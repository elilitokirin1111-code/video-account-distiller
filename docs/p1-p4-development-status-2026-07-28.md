# P1–P4 开发进度对标（2026-07-28）

## 判定口径

- `已实现`：代码、离线合同测试和项目校验均有证据。
- `部分实现`：核心能力存在，但缺少真实外部验收、完整数据契约或浏览器验收。
- `未实现`：未找到对应代码、工件或自动化测试证据。
- 真实外部调用、付费 Provider 和账号采集不以 Mock/Fixture 代替验收。

## 当前工程基线

- 最近更新：2026-07-30。
- 分支：`integration/unified-product`。
- 全量测试：250 passed。
- 总覆盖率：85.13%。
- Ruff、Ruff format、mypy：零错误。
- 真实项目只读校验：
  `video-account-distiller-projects/douyin-b3PQxQdIxN8` 为零错误、零警告。

## P1：真实全链路限量验收 — 部分实现

### 已有证据

- 真实项目包含 1 个账号、20 条视频、20 条指标快照和 200 条限量评论。
- 20/20 视频有 Whisper 转写、媒体特征和语义分析，当前视频语义覆盖率为 100%。
- 已生成账号报告、蒸馏报告、证据索引、评论结论和限制说明。
- 报告明确保留未知项，机器数据未把缺失值写成 0。
- `validate_project(..., persist=False)` 返回 0 error / 0 warning。
- 已生成 OpenKB 脱敏导出文档和 manifest。
- 新生成的账号报告包含 `data-gaps.json`，逐字段区分公开数据、授权私域数据、
  模型推断和未知来源，并保留记录级证据反链。

### 未完成验收

- `account_snapshots.parquet` 只有 1 个时间点，尚不能验收第二次快照增长计算。
- OpenKB 只有导出工件；没有真实服务同步、重复同步和查询回执。
- 真实验收需要再次确认账号授权、OpenKB 服务配置和第二次采集时间。

## P2：GPT 分析 Provider — 代码主体已实现，真实验收未完成

### 本轮完成

- 接入 OpenAI Responses API，使用 `text.format.type=json_schema`、`strict=true` 和
  `store=false`，并进行本地 Pydantic 复验。
- 项目默认关闭云上传；每次调用同时要求数据外发与费用确认。
- API Key 仅从 API 服务环境变量 `OPENAI_API_KEY` 读取，HTTP/Web 请求体不再接收 Key。
- 新增调用前预览：模型、模板、推理强度、脱敏数据范围、请求指纹、费率快照和保守费用上限。
- 保存请求摘要、上下文/Prompt/Schema 哈希、响应 ID、输出哈希、实际 token 用量、
  返回模型版本和基于版本化费率的费用估算。
- 新增固定问题集 `evaluation.json`：引用完整性、证据白名单、数字幻觉复核、
  结论稳定性和派生分析写入边界。
- GPT 工件只写入 `analyses/gpt/`，不修改 Rule/Rubric。
- 同一上下文、模型、模板和 Prompt 版本复用稳定工件，避免重复计费。

### 未完成验收

- 尚未使用真实 `OPENAI_API_KEY` 完成一次受控 Responses API 调用。
- 结论稳定性当前会诚实返回 `insufficient_runs`；还需要至少两次独立、同上下文评估。
- 费用为基于响应用量和冻结费率的估算，最终账单仍以 OpenAI 为准。
- 尚未形成跨多个固定账号 Fixture 的长期回归基线。

## P3：授权私域数据 — 代码契约已实现，真实私域样本未验收

### 已有证据

- `MetricSnapshot` 已覆盖 impressions、观看时长、完播、主页访问、涨粉、点击、
  线索、订单和收入，全部为可空字段。
- 已有授权导出 manifest、字段映射和协作导入基础设施。
- 缺失数值在核心模型与统计中保持 `None`。
- 创作者指标映射版本升级为 v2，覆盖展现、观看时长、完播、主页访问、涨粉、
  点击、线索、订单和收入，并包含中文抖音创作者字段别名。
- 新增 `AudienceProfileSegment` 1.0.0 粉丝画像契约与
  `normalized/audience_profiles.parquet`。
- 授权导入收据保存 `authorized_private` 来源分级和授权证明 ID。
- 账号报告与 `data-gaps.json` 统一展示 `public`、`authorized_private`、
  `model_inferred` 和 `unknown`；未导入字段显示“未知”。
- 新增版本感知的粉丝画像转换器：长表保持原样，已识别的抖音创作者后台宽表按
  性别、年龄和城市等级展开为分段长表；百分号、百分数和 0～1 比例均有确定性单位转换，
  不支持的版本或未识别字段会显式失败。
- Web 可同时上传授权 manifest 与数据文件，写入前校验 read 授权和 SHA-256；
  新增只读数据浏览页，可查看粉丝画像并按来源分级筛选标准化记录和导入收据。

### 未完成

- 仍需用明确授权的真实创作者后台导出验证字段别名和单位，当前自动化验收使用本地 Fixture。
- 当前宽表转换器只接受已声明的抖音导出版本族和字段白名单；拿到真实样本后仍需按实际版本
  扩充别名、桶边界和单位规则，不能把 Fixture 兼容性视为真实平台验收。

## P4：工作台与生产化 — 本轮生产稳定性主体完成，自动化回归与发布审计仍有缺口

### 已有证据

- Web 已覆盖初始化、快速采集、报告、设置、OpenKB 和 GPT 入口。
- Web 已增加授权私域导入、粉丝画像浏览和来源分级筛选入口。
- SQLite 持久任务支持原子认领、租约、并发配额、安全取消、显式重试和失败恢复。
- 新增隔离任务恢复演练：在临时 SQLite 队列中完成检查点、租约过期、中断恢复、
  重试继承和最终完成，并验证不接触生产任务库；可通过
  `POST /api/doctor/task-recovery-drill` 独立运行。
- API 合同覆盖初始化、采集、导入、分析、报告、设置和任务控制。
- TikHub 采集后会保存不含密钥和原始值的 `drift-report.json`，对解析、账号、作品和评论
  的映射关键路径做确定性合同检查；字段增加允许兼容，缺失/类型变化会形成告警。
- MediaCrawler doctor 已报告固定上游提交、实际提交、Python 约束、关键文件、uv、桥接脚本、
  Chrome/Edge 可执行文件、用户目录和登录状态边界；仅能证明配置就绪，不会把目录存在误报为已登录。
- 已用真实浏览器完成一次本地验收：初始化、CSV 上传导入、标准化、数据浏览、指标计算、
  异步报告、设置连接和采集预检均成功；采集预检工件确认 `dry_run=true`，未调用真实 Provider。
- 浏览器验收发现同步指标计算响应此前没有可见反馈，现已统一展示成功、失败和结果摘要。
- 发布流程已有版本一致性、隐私文档、许可证、构建产物和 SHA-256 校验。
- CLI 公共运行时、备份命令和发布审计已拆为独立模块；OpenKB 与采集批次校验也已从
  总校验器拆出并保持公共 `validate_project` 兼容。
- 新增完整项目备份、逐文件 SHA-256 校验、只恢复到新目录和隔离回滚演练。
- 新增可机器读取的 RC 审计，检查版本、许可证、隐私文档、wheel/sdist 内容和校验和。
- 已在全新 Python 3.11.15 环境安装本轮 wheel，23 步安装后验收全部通过，
  最终项目 0 error / 0 warning。

### 未完成

- 本轮浏览器证据是受控本地验收，尚未固化为每次 CI 自动执行的浏览器回归套件。
- TikHub 漂移检测已接入采集工件和任务告警，但仍需真实响应样本和定时运行策略验证告警信噪比。
- MediaCrawler 诊断不会读取 Cookie 或伪造登录结论；真实登录、浏览器拉起和采集仍需授权账号验收。
- CLI/校验器已完成第一层拆分；`validate_project` 的媒体、闭环和协作段仍可继续细分。
- 回滚演练已自动化；正式发布前仍需在冻结提交上重建工件、重新冻结校验和并人工签字。

## 建议的下一验收顺序

1. 在明确授权和可接受费用后，对现有真实项目完成 OpenKB 同步、重复同步和查询。
2. 间隔采集第二次账号快照，运行增长汇总并再次执行零错误校验。
3. 使用环境变量中的真实 Key 做一次最小 GPT 调用，再做第二次独立稳定性评估。
4. 使用真实创作者后台导出验证 v2 映射和现有粉丝画像转换器，并按真实版本扩充字段白名单。
5. 将本轮浏览器验收固化为 CI 回归，使用真实 TikHub 响应校准漂移合同，再拆分 CLI/validator。
