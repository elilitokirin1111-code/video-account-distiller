# Douyin mapping

Use only user exports. Common aliases include `sec_uid`/`uid`, `aweme_id`, `create_time`,
`play_count`, `digg_count`, `comment_count`, `share_count`, and `collect_count`. Map promoted data
only when the export explicitly supplies it. Do not infer paid traffic solely from high views.
