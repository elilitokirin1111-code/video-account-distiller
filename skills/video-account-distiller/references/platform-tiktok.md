# TikTok mapping

Use only user exports. Common aliases include `author_id`, `unique_id`, `video_id`/`item_id`,
`play_count`, `digg_count`, `comment_count`, `share_count`, and `collect_count`. Do not automate login
or collection.
