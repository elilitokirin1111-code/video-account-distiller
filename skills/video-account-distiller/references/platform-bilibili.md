# Bilibili mapping

Use only user exports. Common aliases include `mid`, `bvid`/`aid`, `pubdate`, `view`, `like`,
`reply`, `share`, and `favorite`. Do not merge coins, favorites, danmaku, and likes into one field.
Unmodeled metrics stay in raw data until a schema version explicitly adds them.
